# 贡献指南

感谢你有兴趣为 Odin 中文学习站（The Odin Project Chinese Learning Companion）做贡献。

## 基本规则

- 发现 bug 或有功能建议，请先开 issue 描述清楚：触发页面、操作步骤、期望行为与实际行为（附截图更好）。
- 提 PR 前请先在 issue 里确认方向，避免大改动被拒；小修复（错别字、样式微调、测试补充）可以直接提。
- PR 保持小而聚焦：一个 PR 解决一件事；提交前请保证既有测试通过（`node tests/*.test.cjs`、`python tests/serve_test.py`）。

## 红线

- **不要提交任何私有信息**：本地绝对路径、账号、token、cookie、个人档案 JSON、未公开截图等一律不进仓库。
- **新增课程内容、视觉资产、学习伙伴角色时必须标注来源**：官方课程引用给官方链接；自绘/AI 生成/第三方素材要在 `SOURCES.md` 登记制作方式与授权边界。未标来源的资产 PR 会被拒绝。
- **不复制第三方内容**：外部文章、视频、书籍只做链接与本站原创导读，不搬运、不镜像、不整篇翻译。
- **中文讲解不使用繁体字**（既有测试有保险断言）。

## 工程原则

- 保持纯静态站：零构建、零运行时依赖、零后端。除非维护者明确决定改变，不要引入 npm 包、框架、打包器或任何需要安装的运行依赖（Python 3 标准库的本地服务脚本除外）。
- 学习数据只存浏览器 localStorage，任何改动不得破坏既有档案兼容（`schemaVersion` 迁移必须是纯增量，storage key 变更必须带兼容迁移与专项测试）。
- 版本号唯一事实源是 `version.js`，不要在别处硬编码版本或品牌字符串。

## License

代码贡献默认按本仓库 [MIT License](LICENSE) 授权；内容与视觉资产遵循 [SOURCES.md](SOURCES.md) 中说明的许可边界（中文讲解内容沿用官方课程的 CC BY-NC-SA 4.0）。提交 PR 即表示你同意上述授权。
